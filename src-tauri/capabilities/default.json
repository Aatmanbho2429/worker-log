{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions the waste log window needs: its own commands, the native save dialog for exports, and opening a finished export in the system viewer.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:event:default",
    "dialog:allow-save",
    "opener:allow-open-path",
    "log:default"
  ]
}
