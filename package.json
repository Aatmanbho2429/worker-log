{
  "name": "ceramic-waste-log",
  "private": true,
  "description": "Desktop waste log for a ceramic sanitaryware line (Tauri + Angular).",
  "scripts": {
    "setup": "npm --prefix web install",
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri",
    "web:start": "npm --prefix web start",
    "web:build": "npm --prefix web run build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
